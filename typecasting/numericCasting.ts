// Enduser enters data --> returns always a string so we need to convert it
// let x: number = parseInt(prompt('Enter a number'));
// console.log(x + 3);

// --> parseFloat for expected decimals

//toString

let courses = ['AJS', 'RJS', 'VUE'];
console.log(courses.toString());

let myBool = false;
let x: string = myBool.toString();
console.log(x);
